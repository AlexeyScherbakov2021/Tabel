<UserControl x:Class="Tabel.Views.Admins.ProfileUserUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Tabel.ViewModels.Admins"
             xmlns:local="clr-namespace:Tabel.Views.Admins"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:ProfileUserUCViewModel/>
    </UserControl.DataContext>

    <Border Background="WhiteSmoke" CornerRadius="3">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="90"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Button Content="Сменить пароль" Width="300" Margin="100,20" Command="{Binding ChangeCommand}" FontSize="16" Style="{StaticResource ButtonStyleShadow}"/>
        </Grid>
    </Border>
</UserControl>
