<Window x:Class="Tabel.Views.SelectDateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Tabel.ViewModels"
        xmlns:local="clr-namespace:Tabel.Views"
        mc:Ignorable="d"
        Title="Выбор даты для печати" Height="455" Width="298" WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:SelectDateWindowViewModel/>
    </Window.DataContext>
    <DockPanel>
        <UniformGrid DockPanel.Dock="Bottom" Columns="2" Margin="2">
            <Button Content="Печать" Margin="4" Command="{Binding PrintCommand}"/>
            <Button Content="Отменить" Margin="4" Command="{Binding CancelCommand}"/>
        </UniformGrid>
        <ListBox Margin="10" ItemsSource="{Binding listDates, StringFormat=dd.mmm.yyyy}" SelectedItem="{Binding SelectedDate}"/>
    </DockPanel>
</Window>
